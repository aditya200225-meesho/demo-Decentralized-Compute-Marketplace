# Activity Log

## 2026-07-08

- Reviewed hackathon rules (React.js / Node.js or Go / SQLite, single Docker image, nginx on 9080 proxying `/api/` to backend on 8090). Confirmed Next.js is not allowed under the contract.
- Created project at `D:\decentralized-compute-marketplace` (sibling `D:\buildathon` is an unrelated, pre-existing project — left untouched). `git init` run, global identity already configured.
- Scaffolded frontend: Vite + React + TypeScript. Installed Tailwind v4 + shadcn/ui component deps. shadcn CLI's network fetch (`ui.shadcn.com`) failed due to this network's TLS-inspecting proxy (self-signed cert error) — hand-wrote `components.json`, `cn()` util, and core components (button, card, badge, tabs, input, label, progress, separator, select, dialog) matching standard shadcn output instead.
- Scaffolded backend: Express + TypeScript + Prisma. `npx prisma --version` hit the same TLS-proxy issue trying to download engine binaries from `binaries.prisma.sh`. Asked the user; they approved scoping `NODE_TLS_REJECT_UNAUTHORIZED=0` to Prisma CLI commands only (matches the user's own pre-existing `strict-ssl=false` npm workaround for this same proxy).
- Designed Prisma schema: Team, Provider, JobTemplate, Job, JobChunk (primary + shadow provider relations for redundant verification), LedgerEntry. Two migrations applied (`init`, `shadow_verification`).
- Prisma 7's new client generator turned out to be ESM/TS-native (uses `import.meta.url`) — converted the whole backend to ESM (`"type": "module"`, bundler-style tsconfig) rather than fighting NodeNext extension-resolution rules. Also discovered Prisma 7 removed `datasource.url` from schema.prisma entirely — switched to the `@prisma/adapter-better-sqlite3` driver adapter constructed at runtime in `db.ts`.
- Built the scheduler (`services/scheduler.ts`): matching, chunk progress simulation, redundant verification (primary vs shadow provider hash compare, ~6% simulated dispute rate), escrow hold/release/refund, reliability recomputation, idle-state jitter (battery/wifi/temp) for the "won't drain your battery" guardrail framing.
- Verified backend end-to-end locally via curl: health check, templates, providers, job submission, and a full job lifecycle observed going PENDING → RUNNING → chunk verification → DISPUTED (one chunk failed verification in the test run, demonstrating the escrow-refund path works) — confirms the whole scheduler loop is functioning.
- Built frontend UI: Providers tab (grid of ProviderCard with hardware/idle-guardrail/reliability info), Jobs tab (JobCard with per-chunk progress + verification icons + dispute/paid messaging, SubmitJobDialog), Templates tab, dashboard stat tiles. Polls via a small `usePolling` hook. Typechecks clean, production build clean.
- Verified full local dev loop: backend on :8090, frontend Vite dev server on :5173 proxying `/api` to the backend — confirmed via curl against the proxy.
- Wrote `Dockerfile` (3-stage: frontend build, backend build w/ native build deps for better-sqlite3, final `node:20-bookworm-slim` + apt-installed nginx runtime), `docker/nginx.conf` (9080 → static + `/api/` proxy to 127.0.0.1:8090), `docker/entrypoint.sh` (runs `prisma migrate deploy`, backgrounds the backend, execs nginx in foreground). **Not yet built or run** — Docker Desktop is not installed on this machine; user chose to continue without Docker for now after a winget install attempt was interrupted.
- Wrote root `.gitignore`, `.dockerignore`, `.env.example`, and this `.agent-memory/` directory.
